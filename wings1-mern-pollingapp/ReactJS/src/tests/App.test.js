import React from "react";
import { render } from "@testing-library/react";
import { BrowserRouter } from "react-router-dom";
import App from "../App";

describe("App routing and layout", () => {
  test.todo("should render header with navigation links");
  test.todo("should render CreatePoll component on '/' route");
  test.todo("should render RegisterVote component on '/register-vote' route");
  test.todo("should render ViewResult component on '/view-result' route");
});
