import React from "react";
import { render } from "@testing-library/react";
import List from "../components/List";

describe("List component", () => {
  test.todo("should render 'No books available.' when list is empty");
  test.todo("should render Card components for each book in data");
});
