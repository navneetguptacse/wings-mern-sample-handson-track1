import React from "react";
import { render } from "@testing-library/react";
import Modal from "../components/Modal";

describe("Modal component", () => {
  test.todo("should not render when show is false");
  test.todo("should render children when show is true");
  test.todo("should call closeHandler when Close button is clicked");
});
