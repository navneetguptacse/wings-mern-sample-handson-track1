import React from "react";
import { render } from "@testing-library/react";
import Form from "../components/Form";

describe("Form component", () => {
  test.todo("should render all form fields and labels for new order");
  test.todo("should display Error component when error prop is provided");
  test.todo("should call inputOnChangeHandler when user types into inputs");
  test.todo("should call onSaveHandler when Save button is clicked");
});
