import React from "react";
import { render } from "@testing-library/react";
import FilterBar from "../components/FilterBar";

describe("FilterBar component", () => {
  test.todo("should render status dropdown and min/max input fields");
  test.todo("should show All as default status option");
  test.todo("should call onFilterChange when any filter input changes");
});
