import React from "react";
import { render, fireEvent } from "@testing-library/react";
import Button from "../components/Button";

describe("Button component", () => {
  test.todo("should render provided label text");
  test.todo("should call onClick handler when clicked");
  test.todo("should default type to button when not provided");
});
