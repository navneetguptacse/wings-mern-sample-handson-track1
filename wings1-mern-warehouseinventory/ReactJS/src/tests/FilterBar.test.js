import React from "react";
import { render } from "@testing-library/react";
import FilterBar from "../components/FilterBar";

describe("FilterBar component", () => {
  test.todo("should render category dropdown and min quantity input");
  test.todo("should call onFilterChange when category is changed");
  test.todo("should call onFilterChange when minQuantity is changed");
});
