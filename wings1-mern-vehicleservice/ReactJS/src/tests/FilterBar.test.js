import React from "react";
import { render } from "@testing-library/react";
import FilterBar from "../components/FilterBar";

describe("FilterBar component", () => {
  test.todo("should render status, start_date and end_date filters");
  test.todo("should call onFilterChange when any filter value changes");
  test.todo("should allow selecting all statuses when no status selected");
});
